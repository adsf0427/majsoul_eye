# majsoul_eye dependencies. Pin against the torch already used by auto/mycv
# (torch==2.5.1 in Akagi) before locking versions — see docs/DESIGN.md §9.

# --- recognition ---
opencv-python>=4.9
numpy>=1.26
torch>=2.5            # tile.model (TileNet) reuse + training
ultralytics>=8.2      # YOLOv8/v11 (+OBB) detector for 河/副露 + external screenshots
onnx>=1.16            # export
onnxruntime>=1.18     # CPU inference deployment
# paddleocr           # OR pytesseract — for player-name text (scores use a digit classifier)

# --- capture ---
mss>=9.0              # fast screen grab for the Windows client
pywin32; sys_platform == "win32"   # window enumeration / DPI handling
# playwright          # browser-version capture (already used by Akagi)

# --- data / labeling / eval (dev only) ---
# label-studio
# fiftyone
